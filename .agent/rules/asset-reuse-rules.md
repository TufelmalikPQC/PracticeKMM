# Asset Reuse Rules

> **Why**: Avoid duplicates, reduce app size.

## ✅ DO

- Check if asset exists first
- Use consistent naming: `ic_`, `img_`, `bg_`
- Use platform-safe formats

## ❌ DON'T

- Don't add duplicate assets
- Don't use arbitrary names
