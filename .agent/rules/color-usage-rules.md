# Color Usage Rules

> **Why**: Theme colors enable dark mode and consistency.

## ✅ DO

- Define in `Color.kt` (light + dark)
- Use `MaterialTheme.colorScheme`

## ❌ DON'T

- Don't hardcode `Color(0xFF...)` in UI
- Don't skip dark mode variants
